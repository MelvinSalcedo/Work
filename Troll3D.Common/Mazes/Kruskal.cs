using System;
using System.Collections.Generic;

namespace Troll3D.Common.Mazes
{
    public class Kruskal
    {
        public void Do()
        {
            //dooropened_ = 0;
            //grid_ = new Grid(width_, height_, roomcount_, roomwidthmin_, roomwidthmax_, roomheightmin_, roomheightmax_);

            //Camera.main.transform.position = new Vector3(width_ / 2.0f, height_ / 2.0f, -5.0f);

            //copywalls_ = new List<Wall>();

            ////for (int i = 0; i < grid_.mazewalls_.Count; i++) {
            ////    if (!grid_.mazewalls_[i].isopened_) {
            ////        copywalls_.Add(grid_.mazewalls_[i]);
            ////    }
            ////}
            ////dooropened_ += GetTotalRoomDoors();

            ////while (dooropened_ < ((width_) * (height_) - 1)) {
            ////    MazeStep();
            ////}
        }
    }

}
